# 成员 6：测试计划与验收记录

## 1. 基线与职责边界

- 验收日期：2026-09-07。
- 当前 Git 基线：`develop`，commit `7515b2df5206e2539b354fd7c84fe1f60643e40e`（PR #13 合并提交）。
- 当前测试分支：`test/checkout-e2e`，已按评审要求 rebase 到上述最新 `develop`。
- 成员 6 负责跨模块 API 测试、验收记录、缺陷跟踪、环境和发布检查；不修改商品、购物车或订单业务规则来绕过失败用例。
- 自动化测试只使用独立数据库 `wemove_test`，不得连接开发、演示或生产数据库。

## 2. 本机验证环境

| 项目                    | 实测结果                                              |
| ----------------------- | ----------------------------------------------------- |
| Windows                 | Windows 11 Home，x64                                  |
| Node.js / npm           | Node.js 24.19.0 / npm 11.17.0                         |
| Docker Desktop          | 4.89.0，WSL 2 Linux 后端                              |
| Docker Engine / Compose | 29.7.2 / 5.5.0                                        |
| PostgreSQL              | `postgres:16-alpine`，`wemove-postgres` 为 healthy    |
| 测试数据库              | `wemove_test`，7 份 migration 已应用，seed 可重复执行 |
| 测试商品数据            | 7 个商品、14 个 ACTIVE 且有库存的 SKU                 |

## 3. 基线检查结果

| 检查项                   | 结果             | 证据摘要                                                                  |
| ------------------------ | ---------------- | ------------------------------------------------------------------------- |
| Prisma generate/validate | 通过             | Prisma Client 生成成功，schema valid                                      |
| 后端 lint                | 通过（换行兼容） | GitHub Linux CI 通过；Windows CRLF 环境以 `endOfLine:auto` 检查无规则错误 |
| 后端 Jest                | 通过             | 6 suites、50 tests 全部通过                                               |
| 后端生产构建             | 通过             | `nest build` 退出码 0                                                     |
| 前端 TypeScript/lint     | 通过             | 类型检查与 ESLint 均退出码 0                                              |
| 前端 Vitest              | 通过             | 6 files、29 tests 全部通过                                                |
| 前端生产构建             | 通过             | Next.js 构建成功，生成 14 个页面/路由                                     |
| GitHub PR #14 修正后 CI  | 通过             | 提交 `f2bb94e` 的三项检查全绿                                             |

## 4. 跨模块 API 测试矩阵

| 编号   | 场景                         | 预期                                               | 结果 |
| ------ | ---------------------------- | -------------------------------------------------- | ---- |
| M6-001 | 健康检查、Swagger、统一响应  | 200，响应含 `success` 与 `request_id`              | 通过 |
| M6-002 | 匿名商品列表、详情与草稿过滤 | 创建 DRAFT 后重新查询列表与详情，均不可见          | 通过 |
| M6-003 | 注册、登录、`/auth/me`       | USER JWT 有效，不返回密码哈希                      | 通过 |
| M6-004 | USER、DEALER、ADMIN 角色边界 | 未登录 401、角色不符 403、合法角色成功             | 通过 |
| M6-005 | SKU 单项、批量及 Dealer 价格 | USER/ADMIN 零售价，DEALER 经销商价，不泄漏内部字段 | 通过 |
| M6-006 | 非法、下架 SKU               | 不存在返回 404，不可售返回 400                     | 通过 |
| M6-007 | 真实 SKU 首次与累计加购      | 服务端定价，数量累计，金额正确                     | 通过 |
| M6-008 | 修改购物车数量               | 数量与小计更新，超库存返回 400 且原数据保留        | 通过 |
| M6-009 | 下单前库存二次校验           | 加购后库存降低时下单返回 400，购物车不被清空       | 通过 |
| M6-010 | 恢复库存后下单               | 使用下单时最新服务端价格，订单总额正确             | 通过 |
| M6-011 | 订单名称、SKU、价格快照      | Product/SKU 后续改名改价不改变历史订单             | 通过 |
| M6-012 | 下单后的购物车与个人订单列表 | 购物车清空，订单可在当前用户列表和详情读取         | 通过 |
| M6-013 | 订单归属隔离                 | 其他用户读取或取消订单返回 403                     | 通过 |
| M6-014 | Admin 订单查询与安全字段     | USER 403、匿名 401、ADMIN 成功且不返回密码哈希     | 通过 |
| M6-015 | Admin 订单状态机             | 合法 `PENDING -> PAID` 成功，非法跨级流转返回 400  | 通过 |
| M6-016 | 空购物车下单                 | 返回 400                                           | 通过 |
| M6-017 | 经销商申请、审批与企业隔离   | 审批后角色生效，非企业成员返回 403                 | 通过 |

## 5. 本轮自动化结果

执行命令：

```powershell
$env:E2E_BASE_URL="http://localhost:3100/api/v1"
node --test tests/api-smoke.test.mjs
```

2026-09-07 在 `7515b2d` 基线上完成评审修正后复跑：7 个子场景全部通过、0 失败，Node 测试报告共 8 项（含父测试），耗时约 9.81 秒。除真实 SKU、服务端价格、库存二次校验外，本轮明确验证 DRAFT slug 不出现在公开列表，并验证 Product.name、SKU 名称和价格快照在商品修改后保持不变。

测试会在 `wemove_test` 创建带时间戳的临时用户、专用 SKU、购物车、订单、经销商申请和企业记录。P1 按接口契约不执行真实库存扣减或预占，本轮验证的是加购和下单时的库存校验。

## 6. 当前结论

成员 2、3 的商品/SKU、购物车、订单和 Admin 订单接口已满足成员 6 的 API E2E 前置条件；本轮关键交易链与权限回归全部通过，未发现新的阻塞缺陷。PR #14 的三项评审意见已在 `7515b2d` 基线上修正，本地后端 50 项、前端 29 项及完整 API E2E 均通过；修正提交 `f2bb94e` 的三项 GitHub CI 全绿，等待成员 1 复审。

## 7. 后续回归触发条件

以下任一内容发生变化时，必须重跑完整 API E2E：

1. Prisma schema、migration、seed 或测试数据库配置变化。
2. 认证、JWT、角色守卫或统一响应结构变化。
3. 商品公开字段、SKU 定价/库存规则变化。
4. 购物车、订单、Admin 订单接口或状态机变化。
5. 前端关键流程准备进入最终浏览器验收。
