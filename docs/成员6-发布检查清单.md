# 成员 6：发布检查清单

本清单用于每次准备合入 `develop`、从 `develop` 合入 `main`，以及最终答辩演示前。检查人应填写 commit SHA、日期和证据链接，不只写“已测试”。

## A. 基线与仓库

- [x] 当前工作基于 Git 仓库最新 `develop`，已记录 commit SHA `57ab810`。
- [ ] 功能通过 Pull Request 合入，没有直接在 `main` 开发。
- [ ] 工作区不包含 `.env`、令牌、数据库备份、日志或 `req_dump.txt`。
- [ ] 新数据库改动同时包含 schema 与 migration，并已由成员 1 review。
- [ ] API/DTO/权限变化已同步 Swagger 和对应前端负责人。

## B. 环境与数据库

- [x] Docker Desktop 使用 WSL 2 Linux 后端。
- [x] PostgreSQL 16 容器曾通过 healthy 检查。
- [x] 6 份 migration 可从已有 P1 数据库顺序升级成功。
- [x] 6 份 migration 可在空的 `wemove_test` 数据库部署成功。
- [x] seed 可重复执行并得到 3 个账号、1 个分类、7 个商品。
- [ ] SKU/Variant 种子数据可支持购物车与订单联调。
- [ ] 迁移与种子步骤已在最终 Git commit 上重新执行。

## C. 自动检查

- [x] `npx prisma validate`
- [ ] 后端 `npm run lint`（代码规则已通过；本机完整脚本受 Git CRLF 检出方式影响，待 Linux CI 确认）
- [x] 后端 `npm test -- --runInBand`（4 suites、25 tests）
- [x] 后端 `npm run build`
- [x] 前端 `npm run typecheck`
- [x] 前端 `npm run lint`
- [x] 前端 `npm run build`
- [ ] `node --test tests/api-smoke.test.mjs`（前一合并基线 6/6；最新 `57ab810` 待 Docker 恢复或 GitHub CI 验证）
- [ ] GitHub CI 在最终 PR/commit 上为绿色。

## D. 关键业务与权限

- [x] 健康检查和 Swagger 返回 200。
- [x] 注册、登录、当前用户查询通过。
- [x] 未登录访问购物车返回 401。
- [x] 普通用户访问管理员/经销商权限端点返回 403。
- [x] 经销商申请、管理员审批、角色更新和企业隔离通过。
- [ ] 匿名商品接口不返回经销商价。
- [ ] 匿名商品列表只返回 ACTIVE 商品。
- [ ] SKU 路由只包含一次 `/api/v1` 前缀。
- [ ] 普通用户 SKU 响应不包含 `dealerPrice`。
- [ ] 真实 SKU 加购使用服务端价格并校验库存。
- [ ] 下单再次校验库存并保存商品、SKU、价格快照。
- [ ] 订单只能由所有者或管理员读取，跨用户访问返回 403。
- [ ] 管理员订单状态只能按允许的状态机转换。

## E. 前端与演示

- [ ] 商品列表和详情展示真实 API 数据，并处理加载/空/错误状态。
- [ ] 登录态刷新和 401 跳转行为正确。
- [ ] 购物车、下单、订单列表关键交互通过。
- [ ] 经销商申请和管理员审批页面通过。
- [ ] 普通用户看不到管理入口，直接访问也被后端拒绝。
- [ ] 桌面与移动端布局完成基本检查。
- [ ] 演示账号、演示步骤和失败备用方案已准备。

## F. 发布结论

当前结论：**不满足最终发布条件**。

最新 `57ab810` 的 Prisma 校验、前端 lint、后端 25 项单元测试和前后端生产构建均已通过，后端代码规则在忽略本机 CRLF/LF 差异后通过；跨模块 API 测试在前一合并基线为 6/6，最新基线等待 Docker 恢复或 GitHub CI 验证。发布前需要关闭商品/SKU 的公开字段与路由问题，把 SKU 服务接入购物车和订单，并等待前端真实页面合入后执行完整浏览器 E2E。
