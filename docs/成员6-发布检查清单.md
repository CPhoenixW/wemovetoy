# 成员 6：发布检查清单

本清单用于每次准备合入 `develop`、从 `develop` 合入 `main`，以及最终答辩演示前。检查人应填写 commit SHA、日期和证据链接，不只写“已测试”。

## A. 基线与仓库

- [x] 当前工作已 rebase 到 Git 仓库最新 `develop@7515b2d`（2026-09-07）。
- [ ] 功能通过 Pull Request 合入，没有直接在 `main` 开发。
- [x] Git 跟踪内容不包含 `.env`、令牌、数据库备份、日志或 `req_dump.txt`。
- [x] 本测试分支未修改 Prisma schema、migration、API、DTO 或权限规则。

## B. 环境与数据库

- [x] Docker Desktop 使用 WSL 2 Linux 后端。
- [x] PostgreSQL 16 容器通过 healthy 检查。
- [x] 7 份 migration 已在独立 `wemove_test` 数据库成功应用。
- [x] seed 可重复执行并得到 3 个账号、1 个分类、7 个商品、14 个可购买 SKU。
- [x] SKU/Variant 数据可支持购物车与订单联调。
- [x] 7 份 migration 与测试数据已在 `7515b2d` 基线上完成 API 回归。

## C. 自动检查

- [x] `npx prisma validate`
- [x] 后端 `npm run lint`（PR #12 Linux CI 通过；本机代码规则检查也通过）
- [x] 后端 `npm test -- --runInBand`（6 suites、50 tests）
- [x] 后端 `npm run build`
- [x] 前端 `npm run typecheck`
- [x] 前端 `npm run lint`
- [x] 前端 `npm run test`（6 files、29 tests）
- [x] 前端 `npm run build`
- [x] `node --test tests/api-smoke.test.mjs`（`7515b2d` 上 7 个子场景全过，约 9.81 秒）
- [x] GitHub CI 在 PR #12 合并前为绿色。
- [x] 成员 6 checkout E2E PR #14 rebase 前的三项 GitHub CI 为绿色。
- [ ] PR #14 评审修正与 rebase 后的三项 GitHub CI 为绿色。

## D. 关键业务与权限

- [x] 健康检查和 Swagger 返回 200。
- [x] 注册、登录、当前用户查询通过。
- [x] 未登录访问购物车返回 401。
- [x] 普通用户访问管理员/经销商权限端点返回 403。
- [x] 经销商申请、管理员审批、角色更新和企业隔离通过。
- [x] 匿名商品接口不返回经销商价。
- [x] 匿名商品列表只返回 ACTIVE 商品。
- [x] 创建 DRAFT 商品后重新查询公开列表，确认不包含其 slug。
- [x] SKU 路由只包含一次 `/api/v1` 前缀。
- [x] 普通用户 SKU 响应不包含 `dealerPrice`。
- [x] 真实 SKU 加购使用服务端价格并校验库存。
- [x] 下单再次校验库存并保存商品、SKU、价格快照。
- [x] 下单后修改 Product.name、SKU 名称和价格，历史订单快照保持不变。
- [x] 订单只能由所有者或管理员读取，跨用户访问返回 403。
- [x] 管理员订单状态只能按允许的状态机转换。

## E. 前端与演示

- [ ] 商品列表和详情展示真实 API 数据，并处理加载/空/错误状态。
- [ ] 登录态刷新和 401 跳转行为正确。
- [ ] 购物车、下单、订单列表关键交互通过。
- [ ] 经销商申请和管理员审批页面通过。
- [ ] 普通用户看不到管理入口，直接访问也被后端拒绝。
- [ ] 桌面与移动端布局完成基本检查。
- [ ] 演示账号、演示步骤和失败备用方案已准备。

## F. 发布结论

当前结论：**API E2E 阶段验收通过，但尚不满足最终发布条件**。

最新 `7515b2d` 基线的数据库、后端、前端和跨模块 API 检查均已通过；PR #14 的公开列表 DRAFT 过滤、Product.name 快照和 rebase 三项评审意见已修正。最终发布前仍需本次修正的 GitHub CI、成员 1 复审合并、完整浏览器/移动端人工验收、演示账号和失败备用方案。
